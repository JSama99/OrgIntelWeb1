Revise Character Realism Pass C2A on the current character-realism-c2-binaries branch.

Do not create a pull request or promote the Hero yet.

1. Correct review-only Hero scale.

The generated GLB is approximately 1.8 units tall, while the existing procedural Hero is approximately 4.67 units tall.

Update loadCharacterAsset() to support candidate-specific scale values:

- Production Hero scale: 1.0
- Hero review scale: 2.6
- Production Tal scale: 1.12
- Future Tal review scale must remain independently configurable

Use the successful candidate’s scale when calling prepareLoadedModel().

Do not change production Hero or Tal scale.

2. Reduce the Hero review glow only.

When the Hero review candidate loads successfully:

- Reduce the player halo scale from 6.5 × 3.2 to approximately 3.6 × 1.8
- Reduce halo opacity to approximately 0.16
- Reduce the player point-light intensity from 14 to approximately 5

Do not change production-character lighting or fallback lighting.

Implement this through a review-success callback or equivalent candidate-specific logic. Do not affect Tal.

3. Correct the Hero head animation pivot.

The head geometry is currently baked in world-space coordinates while HERO_Head has no joint translation. Rotating HERO_Head can therefore rotate around the floor origin.

Update the generator so:

- HERO_Head is positioned at the actual head/neck pivot
- Head-related mesh children use inverse local offsets so their final static placement remains unchanged
- Hero_Idle, Hero_Observe, and Hero_Console rotate around the head/neck area
- Static appearance and total character height remain unchanged

Regenerate and revalidate the Hero GLB.

4. Remove this accidental repository file:

attached_assets/Pasted-Begin-Character-Realism-Pass-C2A-OrgIntel-Architect-Her_1784589584162.txt

Do not remove any other attached asset.

5. Update CHARACTER_REALISM_BRIEF.md with:

- Review runtime scale: 2.6
- Effective displayed height: approximately 4.68 scene units
- Reduced review-only halo and light settings
- Corrected head animation pivot
- New file size and SHA-256 after regeneration

6. Validate:

- Khronos validation: 0 errors
- Hero review loads successfully
- Production Hero fallback remains scale 1.0
- Tal production remains scale 1.12
- No production binary changes
- git diff --check passes

7. Commit and push the corrections to character-realism-c2-binaries.

Report the new commit, GLB SHA-256, validation result, and exact review URL.